<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ImageClassificator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageClassificator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page>

        <Page.Resources>
            <DataTemplate x:Key="NavViewMenuItemTemplate" x:DataType="local:Category">
                <NavigationViewItem Content="{x:Bind Name}">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="{x:Bind Icon}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </DataTemplate>
        </Page.Resources>
        
        <NavigationView
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            MenuItemsSource="{x:Bind Categories, Mode=OneWay}"
            MenuItemTemplate="{StaticResource NavViewMenuItemTemplate}"
            PaneTitle="Image Classificator"
            SelectionChanged="NavView_SelectionChanged"
            x:Name="NavView"
            >
            <Frame x:Name="ContentFrame"/>
        </NavigationView>

    </Page>
  
</Window>
